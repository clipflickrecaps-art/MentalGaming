// Fullfix13: legacy /dashboard disabled to avoid old admin UI.
// /dashboard is handled by adminFullFix8.js with reply keyboard only.
module.exports = function registerLegacyDashboardDisabled(bot) {
  // no-op intentionally.
};
