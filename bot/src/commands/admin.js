// Fullfix9: legacy inline admin panel disabled.
// The active admin UI is reply-keyboard only and is registered in adminFullFix8.js.
module.exports = function registerLegacyAdminDisabled(bot) {
  // no-op intentionally
};
