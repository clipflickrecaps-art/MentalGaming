// Fullfix13: legacy inline admin game/config panel disabled.
// Use the reply-keyboard Admin Panel from adminFullFix8.js instead.
module.exports = function registerAdminGameConfigDisabled(bot) {
  // no-op intentionally to prevent inline keyboard conflicts.
};
